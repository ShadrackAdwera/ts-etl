apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ts-fileupload-database-pvc
spec:
  resources:
    requests:
      storage: 128Mi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
